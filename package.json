{
  "name": "workit-landing-page",
  "version": "1.0.0",
  "description": "A solution to a Workit Landing Page challenge provided by Frontend Mentor",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-dev": "sass --watch --source-map scss/main.scss:css/compiled_style.css",
    "sass-prod": "sass --no-source-map --style=compressed scss/main.scss:css/compiled_style.css"
  },
  "author": "Levi Wright",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.79.4"
  }
}
